steps:
  # Step 1: Install libraries and set up the database
  - name: 'python:3.11-slim'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        pip install -r requirements.txt
        python database_setup.py
    # Pass environment variables to this step
    env:
      - 'DB_URL=${_DB_URL}'
    id: 'Setup'

  # Step 2: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'me-west1-docker.pkg.dev/$PROJECT_ID/whatsapp-bot-repo/calendar-bot:latest'
      - '.'
    id: 'Build'

  # Step 3: Push the image to the repository
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'me-west1-docker.pkg.dev/$PROJECT_ID/whatsapp-bot-repo/calendar-bot:latest'
    id: 'Push'

  # Step 4: Deploy the new version to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'calendar-bot'
      - '--image'
      - 'me-west1-docker.pkg.dev/$PROJECT_ID/whatsapp-bot-repo/calendar-bot:latest'
      - '--region'
      - 'us-central1'
      - '--quiet'
      - '--set-env-vars'
      - "DATABASE_URL=${_DATABASE_URL},META_VERIFY_TOKEN=${_META_VERIFY_TOKEN},META_ACCESS_TOKEN=${_META_ACCESS_TOKEN},META_PHONE_NUMBER_ID=${_META_PHONE_NUMBER_ID},GOOGLE_CLIENT_ID=${_GOOGLE_CLIENT_ID},GOOGLE_CLIENT_SECRET=${_GOOGLE_CLIENT_SECRET},FLASK_SECRET_KEY=${_FLASK_SECRET_KEY},GEMINI_API_KEY=${_GEMINI_API_KEY},GOOGLE_CLOUD_PROJECT=${_GOOGLE_CLOUD_PROJECT}"
    id: 'Deploy'

# General options
options:
  logging: CLOUD_LOGGING_ONLY